# HB-7: Unit Testing

* Add tests covering the whole `OnlineBankService` functionality
* Including `AccountService`, `AccountRepository` and the validators
* Decide up front:
  * what the tests should look like and which test framework to use
  * whether to use mocks or other test doubles
  * whether the tests are to be executed as part of the task
