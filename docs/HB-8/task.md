# HB-8: Log Transformation

* The current logs are plain unformatted text
* Create a script converting the logs into JSON format
* Decide up front:
  * what the script should look like in general
  * how it should behave for unexpected input
* Logs can be found in git branch `progress/HB-8`